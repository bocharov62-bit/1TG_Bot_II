<<<<<<< HEAD
# Telegram Bot с AI

Простой Telegram бот, который отвечает на вопросы пользователей с помощью локальной AI модели (gemma3:1b через Ollama).

## Особенности

- 🤖 Работает локально через Docker
- 💬 Отвечает на любые текстовые вопросы
- 🧠 Использует gemma3:1b для генерации ответов
- 📝 Краткие неформальные ответы (1-2 предложения)
- 🐳 Docker Compose для простого развертывания

## Требования

- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd bot_primeta
```

### 2. Настройка конфигурации

Скопируйте `env.example.txt` в `.env` и заполните:

```bash
cp env.example.txt .env
```

Отредактируйте `.env`:
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=gemma3:1b
OLLAMA_TIMEOUT=30
```

### 3. Запуск через Docker Compose

```bash
docker-compose up -d
```

Это запустит:
- Ollama сервис (внутренний порт 11434, внешний 11435)
- Telegram бот

### 4. Загрузка модели AI

После запуска Ollama загрузите модель:

```bash
# Для Windows PowerShell (без -it)
docker exec bot_primeta_ollama ollama pull gemma3:1b

# Для Linux/Mac (с -it)
docker exec -it bot_primeta_ollama ollama pull gemma3:1b
```

### 5. Проверка работы

Проверьте логи:
```bash
docker-compose logs -f bot
```

## Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Задайте любой вопрос
4. Получите краткий ответ от AI

## Структура проекта

```
bot_primeta/
├── bot/
│   ├── handlers/      # Обработчики сообщений
│   ├── services/      # Сервисы (AI)
│   ├── utils/         # Утилиты (логирование)
│   ├── config.py      # Конфигурация
│   └── main.py        # Точка входа
├── logs/              # Логи бота
├── Dockerfile         # Образ для бота
├── docker-compose.yml # Конфигурация Docker Compose
├── requirements.txt   # Python зависимости
└── README.md          # Этот файл
```

## Остановка

```bash
docker-compose down
```

## Логи

Логи сохраняются в папке `logs/` и также выводятся в консоль.

## Разработка

Для локальной разработки без Docker:

1. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Убедитесь, что Ollama запущен локально:
```bash
ollama serve
```

4. Загрузите модель:
```bash
ollama pull gemma3:1b
```

5. Обновите `.env`:
```
OLLAMA_URL=http://localhost:11434
```

6. Запустите бота:
```bash
python bot/main.py
```

## Лицензия

MIT
